
{
  "name": "dealers_choice_spa",
  "version": "1.0.0",
  "description": "",
  
  "main": "index.js",
  
  "scripts": {
    "build": "webpack",
    "build:dev": "npm run build -- --mode=development --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npm run build:dev & nodemon server --ignore src/ --ignore dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "faker": "^5.5.3",
    "pg": "^8.7.1",
    "sequelize": "^6.12.0-alpha.1",
    "webpack": "^5.64.2",
    "webpack-cli": "^4.9.1"
  },
  "devDependencies": {
    "axios": "^0.24.0",
    "nodemon": "^2.0.15"
  }
}
